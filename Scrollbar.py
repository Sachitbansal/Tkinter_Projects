# from tkinter import *
# from tkinter import ttk
#
# root = Tk()
# root.geometry("300x300")
#
# text_editor = Text(root, width=30, height=10, font=("TImes", 15), wrap=WORD)
# text_editor.grid(row=0, column=0)
# scroll = ttk.Scrollbar(root, orient=VERTICAL, command=text_editor.yview)
# scroll.grid(row=0, column=1, sticky=N + S)
# text_editor.config(yscrollcommand=scroll.set)
#
# root.mainloop()

